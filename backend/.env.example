# ==============================================================================
# OPERRA OPERATIONS ERP - BACKEND PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================================================

# Server Network Port (Default: 5000)
PORT=5000

# Environment Mode ("production" or "development")
NODE_ENV="production"

# PostgreSQL Database Connection String (Supabase / Managed Postgres with SSL)
# MANDATORY FOR SUPABASE PRODUCTION: Include `?sslmode=require` at the end of the URL
DATABASE_URL="postgresql://postgres:operraayush@db.teooyzzaacnbhftgtakc.supabase.co:5432/postgres?sslmode=require"

# JWT Token Signing Secret Key (Change to a strong random string in production)
JWT_SECRET="e9a4f21b7c8d9e0f3a6b5c4d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3f"


# Frontend Production Origin (Optional: Restricts CORS to your Vercel deployment URL)
FRONTEND_URL="https://frontend-two-alpha-19.vercel.app"
